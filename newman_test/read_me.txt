Oluşturduğum testi export ederek json dosyası oluşturdum.
node.Js indiriniz.
Npm (Node package manager) indiriniz.
node -version kontrolu
npm --version kontrolu
PC reboot.
npm install -g newman
Bu işlemleri yapınız.

CMD ekranında dosyanın olduğu konuma gidin.
cd desktop
cd newman_test
newman run dosyanınadı.

Test sonuclarını görebilirsiniz.